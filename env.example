# ── Nexora Backend — Environment Variables ──────────────────────────────────
# Copy this file to .env.local for local development
# In Vercel: add each variable via Dashboard → Settings → Environment Variables

# ── X (Twitter) OAuth 2.0 App Credentials ───────────────────────────────────
# Get these from https://developer.twitter.com → Your App → Keys and Tokens

X_CLIENT_ID=YOUR_X_CLIENT_ID_HERE
X_CLIENT_SECRET=YOUR_X_CLIENT_SECRET_HERE

# OAuth 2.0 Callback URL — must match exactly what you set in X Developer Portal
# Format: https://YOUR_DOMAIN.vercel.app/api/auth/callback
X_REDIRECT_URI=https://nexora.vercel.app/api/auth/callback

# ── Frontend URL ─────────────────────────────────────────────────────────────
# Where to redirect after OAuth (your Nexora frontend)
FRONTEND_URL=https://nexora.vercel.app

# ── JWT Secret ───────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Keep this VERY secret — it signs user session tokens!
JWT_SECRET=GENERATE_A_STRONG_RANDOM_STRING_HERE

# ── Quest Targets ─────────────────────────────────────────────────────────────
# X User ID of YOUR project's account (the account users need to follow)
# Find it at: https://tweeterid.com or via X API
TARGET_ACCOUNT_ID=1234567890

# Tweet ID of the specific tweet users need to RT / Like / Comment on
# Get the ID from the tweet URL: twitter.com/i/status/THIS_NUMBER_HERE
TARGET_TWEET_ID=9876543210123456789
